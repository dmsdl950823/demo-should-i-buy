# 살까말까 — 프로젝트 의도

## 목적
사용자가 구매 조건을 입력하면 Jev(TypeSafe AI System One)가 `BUY / WAIT / SKIP`과 confidence를 반환하는 작은 Next.js 웹앱을 만든다. Jev의 구조화된 판단을 직접 체험하는 24시간 MVP이며, 구매 정답이나 재무 상담을 제공하는 서비스가 아니다.

## 작업 위치와 현재 상태
- 작업 저장소: `/Users/jeje/Desktop/Private/demo-should-i-buy`
- 로컬 저장소 이름은 유지한다. 원격 저장소 이름은 기존 맥락의 `should-i-buy`와 다를 수 있으므로 실제 remote를 확인한다. 임의로 변경하거나 새 저장소를 만들지 않는다.
- 현재 단계: 계획 문서 작성 완료 후 실행 대기.
- 이 문서 작성은 코드 작업, 예약 자동화, 배포 시작을 의미하지 않는다. 사용자가 실행을 지시한 시점부터 진행한다.

## 사용자 흐름
1. 직접 입력하거나 프리셋을 선택한다.
2. 제품명, 가격, 이번 달 여유자금, 현재 비슷한 제품 상태, 사용 빈도, 필요도를 입력한다.
3. 판단 버튼을 누르면 서버 API route가 Jev를 호출한다.
4. 선택 결과와 confidence를 표시한다. 입력을 바꾸면 다시 판단할 수 있다.

## 포함
- Next.js App Router + TypeScript, 단일 메인 화면, 한국어 UI
- 입력 프리셋 4~5개, 모바일 대응, loading/error/재시도
- 서버 전용 Jev 호출과 API 키, 입력 및 응답 검증
- 실제 API 최소 호출 확인, 핵심 테스트, README, 배포 준비

## 제외
로그인, DB, 히스토리 저장, 실제 상품 검색·가격 비교, 결제, 추천 상품, GPT 설명 생성. 별도 관리 화면과 복잡한 인프라도 추가하지 않는다.

## 구현 원칙
- 공식 사이트에서 연결된 최신 공식 문서와 SDK를 검증한다. 이전 대화의 SDK 이름·endpoint·응답 예시는 검증된 규격이 아니다.
- 실제 Jev 최소 호출 성공이 앱 구현의 선행 조건이다. 키나 접근 권한이 없으면 해당 단계를 BLOCKED로 기록하고 사용자에게 필요한 설정을 알린다. 가짜 confidence로 성공을 대체하지 않는다.
- confidence는 공식 응답 의미에 따라 매핑한다. 정답률이나 구매 만족 확률로 표현하지 않는다.
- 키는 서버 환경변수로만 관리하고 커밋, 브라우저 번들, 응답, 로그에 노출하지 않는다.
- MVP에 필요한 가장 단순한 구현을 선택한다. 배포 준비와 실제 공개 배포를 구분한다.

## 완료 기준
실제 Jev 응답이 입력 → 서버 → 결과 화면까지 연결되고, 4~5개 프리셋과 모바일 화면, 오류 처리가 작동한다. 핵심 테스트·타입 검사·프로덕션 빌드를 통과하고 README로 재실행 및 배포 설정이 가능해야 한다. 미검증 항목은 완료로 표시하지 않는다.

## 실행 방법
`todo/README.md`의 순서와 의존성을 따른다. 실행 지시 후 메인 에이전트가 통합하고, 가능한 경우 Jev/API, schema, UI, QA를 서브에이전트에 분담한다. 24시간은 실행 시작 이후의 최대 작업 범위이며, 시간 경과만으로 완료 처리하지 않는다.
